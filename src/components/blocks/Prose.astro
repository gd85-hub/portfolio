---
interface Props {
  heading?: string;
  body: string;
}

const { heading, body } = Astro.props;
---

<section>
  {heading && <h2 class="mb-3 t-sub text-ink">{heading}</h2>}
  <p class="t-body text-body">{body}</p>
</section>
